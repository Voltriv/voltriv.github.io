Credential entries are written here by the local admin UI at /admin/ (dev only).
One JSON file per credential; the filename is the entry id.
This file keeps the directory tracked while the collection is empty.
